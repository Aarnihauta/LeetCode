namespace TrueLeetCode.Leetcode.HashTable;

//https://leetcode.com/problems/largest-rectangle-in-histogram/
public class L84
{
    public int LargestRectangleArea(int[] heights)
    {
        //var stack = new Stack<(int height, int index)>();
        //int max = 0;
        //for (int i = 0; i < heights.Length; i++)
        //{
        //    var current = i;
        //    while (stack.Any() && stack.Peek().height > heights[i])
        //    {
        //        var rectangle = stack.Pop();
        //        current = rectangle.index;
        //        max = Math.Max(max, (i - current) * rectangle.height);
        //    }

        //    stack.Push((heights[i], current));
        //}

        //while (stack.Any())
        //{
        //    var t = stack.Pop();
        //    max = Math.Max(max, t.height * (heights.Length - t.index));
        //}

        //return max;

        //TODO: L84
        return 0;
    }
}
